services:
  web:
    environment:
      MODE: production
      DB_HOST: postgres
      ALLOWED_HOSTS: "0.0.0.0 127.0.0.1 localhost"
    ports:
      - "8000:8000"

  postgres:
    ports:
      - "5432:5432"

  memcached:
    ports:
      - "11211:11211"
